# Plebian-OS 0.1.9

Prepared 2026-08-12.

Plebian-OS 0.1.9 is the coordinated stack release after 0.1.8. It combines
Plebian-OS, Pleb, Kilix, and Kilix 95 over one pinned Debian installer and
snapshot, terminal engine, Go toolchain, optional desktop sources, PDF tools,
and offline voice closure.

## Publication status

The 0.1.9 release is not published. Local annotated candidate tags may exist,
but there are no published coordinated release tags, no release ISO, and no
fresh-install or strict-media acceptance result. The adjacent-release upgrade
gate described below has passed. This file freezes the source candidate; it is
not an artifact acceptance record by itself.

The complete `releases/0.1.9.env` closure now freezes the final four component
commits and all immutable build inputs. It is the candidate input record, not a
claim that the resulting artifacts have passed acceptance.

Publication requires a strict ISO built from the final tagged closure, its
checksum and source archive, both BIOS and UEFI exact-ISO installs, the
automated installed-VM acceptance result, all operator checks in
[`RELEASING.md`](../RELEASING.md), and the six-gate in-place upgrade run in
[`UPGRADING.md`](../UPGRADING.md).

## Install and upgrade policy

Supported upgrade source: **0.1.8**, the immediately previous coordinated
release. The upgrade must preserve user files, application and game state,
provider and desktop state, shared settings, operator session choices, custom
wallpaper, and layout.

No direct skip from an earlier release is supported. Systems older than 0.1.8
must follow the published adjacent-release path or install fresh. A failed or
missing 0.1.8 to 0.1.9 upgrade-acceptance run blocks publication.

### Selecting the 0.1.9 closure

Run the 0.1.9 selector extracted from the target tag as the Pleb user, never
through `sudo`. The installed 0.1.8 copy predates 0.1.9's exact uv-pin and
per-component ancestry checks, so it is not authoritative for this hop. The dry
run validates the target tag and complete closure without writing; the final
command atomically selects it:

```sh
SRC="$HOME/.local/gpu_terminal/sources/plebian-os"
SEL="$(mktemp)"
git -C "$SRC" fetch --force origin 'refs/tags/v0.1.9:refs/tags/v0.1.9'
git -C "$SRC" show v0.1.9:provision/plebian-os-select-closure.sh >"$SEL"
bash "$SEL" 0.1.9 --source "$SRC" --dry-run
bash "$SEL" 0.1.9 --source "$SRC"
```

Only after selection succeeds, with nothing privileged in between, run:

```sh
plebian-os-update --restart
```

The selector reads `releases/0.1.9.env` from the published `v0.1.9` tag,
validates every exact component and dependency pin, reports ancestry for each
component, and changes only release-controlled session keys. The uv enable
flag, exact version, and installer checksum are installed closure keys for this
purpose, not image-only metadata. In the same transaction the selector verifies
and installs the exact tagged selector and target updater. The published 0.1.8
updater therefore does not have to discover 0.1.9's new twelfth OS payload,
dependency policy, or final-provenance behavior while that older process is
already running. The recovery record retains the previous closure and both
previous tools (or either absence) for rollback. The target updater installs the
selected dependencies, including Evince and uv 0.12.3, and atomically records
the final package, source, tool, and apt-index provenance before committing.
Beginning with an installed 0.1.9 system, the selector is available on `PATH`
as `plebian-os-select-closure` for inspection, rollback, and same-release
reselection. Keep `$SEL` until the upgrade succeeds, then `rm -f "$SEL"`.

### Upgrade acceptance result

**PASSED.** All six gates in [`UPGRADING.md`](../UPGRADING.md) ran in order on
2026-08-13 in a VirtualBox BIOS VM installed from the published
`plebian-os-0.1.8-amd64.iso`. No direct skip was tested or is supported; this
record covers only 0.1.8 to 0.1.9.

**Tested start — 0.1.8.** The published source ISO hashes to
`da630c2d48fda87532a07a13125f9fae818e716d3dfe5892c040a8e801995e89`.
Its installed `/etc/plebian-os/build-info.env` hashes to
`49b60a47c16a619ce2e2c4dab60378d177b150536e8669393dd5898068536aec`.
The clean starting checkouts were Plebian-OS
`98d6f5b86a813d30245f0067c1a31afa3a8adf40`, Pleb
`3878edc63d74cb5e4c569b8190fd604e4b7f4bc2`, Kilix
`85785f60c65deace4f031b2fffccd5d157c122f8`, and Kilix 95
`dc185f1bb5fbc2e8e051f7080b4f5779c5bbcb2e`. All installed version commands
reported 0.1.8; the selector and uv were absent, and Evince was not installed.

**Tested target implementation — 0.1.9.** The Plebian-OS candidate was
`5336d9f3f56fe8ddcd0713477c349d8cafe2851f`, with the Pleb, Kilix, Kilix 95,
and Kilix Voice commits frozen below. The exact selector and updater hashes
were respectively
`336c58b7918dbc3b424dab8c3c9ae5a3774258d3992512be4032c0501cd15ba4`
and `e9acb610e27a4c01e60e68cc97a098c5546970d4eb967644df549bba0b40e08c`.
This release-record change adds no shipped code or closure input; the final
tagged candidate is still subjected to the strict artifact and fresh-install
lanes below.

**Candidate-tag substitution.** The coordinated tags were deliberately still
unpublished. The online selector therefore refused the missing remote
`v0.1.9`, as it must. A complete annotated-tag bundle with SHA-256
`64338a712243f39e982dcfea0fed288b404939952660c6fc10f1304776bf5566` was
imported into the installed source object's store, the exact component commits
and complete histories were preloaded without moving a checkout, and the
target selector ran with `--offline`. It proved all four moves were forward,
validated every manifest pin, and left the source working tree untouched.
Publishing the tag removes this candidate-only substitution and makes the
documented fetch command work verbatim.

**Preservation and rollback — gates 2 and 3.** Thirteen file sentinels covered
ordinary user data, a game save, provider data/state, component state, a
transcript, desktop layout, a custom wallpaper, and desktop state. Thirteen
operator choices covered appearance, logging, audio, game availability,
unknown future-schema keys, kiosk behavior, and optional-provider policy.
Before the updater ran, an induced selector failure after deploying its updater
slot restored the old session and updater byte for byte, restored selector
absence, and left no recovery residue. The real updater was then failed at its
`os-layer` boundary. Its outer transaction restored the complete 0.1.8 payload,
the three provenance files, and every sentinel and setting. uv and Evince
remained absent. The selector's `--rollback` then restored the original session
and 0.1.8 updater byte for byte and removed the target selector; the resulting
full deterministic audit equalled the gate-2 capture.

**Successful upgrade — gate 4.** From that restored baseline, one detached
command ran the target selector and `plebian-os-update --restart` adjacently,
with no privileged operator action between them, followed by a reboot. After
boot, all four checkouts and all version commands identified 0.1.9; the
recursive Kilix graph was clean and its engine generation coherent. The exact
target selector and updater were installed root-owned and mode `0755`. Evince
48.1 and its Poppler/GTK runtime were installed from the pinned Debian snapshot,
and root-owned uv/uvx reported `0.12.3 (x86_64-unknown-linux-gnu)`. Final
package, source/tool, and apt-index provenance hashes were respectively
`36f7e3e7a571a3f0f8ca9d1184c7de6e993ae5a1a885944798fdb8a4fc54a54a`,
`f971c7306d3cc9668b61e67245203cd68c8e4423814f1c258e1bdf7fa18bbe12`,
and `a013b421102ba829defe3691e432e1f4fc225786eac066951c454d16ac6ffdc0`.
Every apt provenance entry named the fixed `20260727T000000Z` snapshot, and no
whole-stack recovery residue remained.

**Pin discipline — gate 5.** Exactly ten release-controlled keys moved: the
version and release, four coordinated source refs, Kilix Voice ref, and the
three newly introduced uv policy/version/checksum keys. The selector added one
comment identifying those new closure keys. Every pre-existing
non-release-controlled session line, all shared settings, every operator choice,
and every sentinel remained byte-identical.

**Evidence record — gate 6.** The checksummed top-level record hashes to
`4852494d54ba24dd182dd1a32d1bcec203530b5dbecf514a493d44f9cc844b95`.
It binds the starting artifact, both closures, the selector/updater bytes, all
five preceding gate results, final provenance, and the explicit absence of a
supported skip. The published 0.1.8 ISO remains the fresh-install baseline and
is the only starting-image evidence; earlier local 0.1.9 candidate ISOs remain
discarded. No earlier local candidate ISO supplies evidence for this closure.

## Highlights

- First-boot retries remain one continuously ordered systemd start job. A slow
  mirror can no longer release LightDM between bounded attempts and make the
  first login enter Debian's fallback session before Pleb is installed. The
  transactional OS-layer update path accepts and preserves the repaired unit.
- Every BIOS installer-menu level now carries the exact 0.1.9 product title,
  including the top-level Advanced and accessible-dark menus and the nested
  dark Advanced menu; remastering fails closed if those upstream titles drift.
- Provisioning and firstboot now transact their OS/Pleb outputs and restore the
  prior bytes, links, modes, and absences after failures or signals.
- Whole-stack update rollback now also treats a lost terminal or SSH output
  pipe as a transactional failure instead of allowing SIGPIPE to bypass the
  EXIT rollback trap.
- Whole-stack rollback now records and restores Kilix's complete recursive
  submodule graph instead of a fixed list. Changed existing modules return to
  their prior commits, target-only top-level and nested modules are removed,
  and initialized, empty, and absent worktree states are preserved. The target
  Pleb layer also enables repository-local recursive checkout after its
  parent-first Kilix reconciliation, so the 0.1.8 updater can unwind this first
  upgrade hop coherently even though it does not know every 0.1.9 submodule.
- Automated acceptance now synchronizes with the updater's detached LightDM
  restart by requiring a new active service invocation within a bounded wait.
  This tests that the restart really happened without sampling its intentional
  stop/start gap as a failure.
- Both installer paths now include SQLite's development headers and linker
  input, allowing the completed catalog-pinned Kilix NVR to clean-build its
  database-backed recorder on the release image.
- Closure selection is installed as part of the validated OS layer and proves
  per-component ancestry before changing the coordinated release pins.
- Fresh images install checksum-verified uv 0.12.3, while the application
  closures retain their documented CPU and non-uv fallbacks.
- Evince provides the native PDF fallback and full document features; Kilix's
  catalog-pinned terminal PDF viewer adds retained, damage-aware presentation
  with a complete Poppler/Cairo CPU raster path.
- The shared application catalog now drives Kilix, Kilix 95, and the other
  desktop providers, including terminal PDF viewing and conversion.
- Kilix Voice now owns one `kilix.speech.models/v1` catalog contract for the
  CLI, dictation TUI, Kilix 95 Settings, built-in WM Settings, and the terminal
  microphone button. Every surface offers explicit lazy installation and
  coherent model/engine defaulting; merely listing or opening settings remains
  download-free.
- Pane chrome completes the independent CPU-load and process-tree memory
  policies, including `auto`, `always`, and `off` modes. On a 0.1.8 upgrade, an
  absent `KILIX_CHROME_PANE_CPU_MODE` key resolves to `auto` without rewriting
  the existing `settings.conf`; the value is persisted only when the operator
  changes it explicitly. This keeps the first 0.1.8 to 0.1.9 transaction
  byte-exact even though the previous release's updater coordinates that hop.
  Beginning with an installed 0.1.9 system, the updater also snapshots shared
  settings and restores them byte for byte after any failed transaction.
- The maintained module/library review completed all 25 targets, all 14 carried
  actions, and all configured provider/consumer publication checks.

## Frozen source closure

The candidate is paired to these exact public branch tips:

- Pleb: `a245fcfdf39d3f5d29b7ded797f8ca251ad851eb`
- Kilix: `866a366b468a47597183aadc98a85a11a3f072d2`
- Kilix 95: `8d0449bd2f63d5ecc526bc40213490d7398a5be7`
- Kilix Voice: `f501409a82bf73b738b14986e12441bce23ec1c6`
- Plebian-OS implementation tested by the upgrade gate:
  `5336d9f3f56fe8ddcd0713477c349d8cafe2851f`; the local candidate tag advances
  only for this release record before the artifact lanes run.

Kilix pins engine commit `304ee7ea70ef15cbe8ea7f6b69a7307d94d456d3`
and shared content catalog commit
`ae82bf87b2489851bc525a4f893d56de7e4bf287`. The catalog and direct installers
resolve the completed NVR, RTSP, and object-detection provider closure to the
same commits.

## Frozen release requirements

The final manifest includes all ordinary release closure fields plus:

- `PLEBIAN_OS_INSTALL_VOICE_MODEL=1`, with a full Kilix Voice 0.1.3 source
  ref whose `kilix-stt --models --json` output passes the
  `kilix.speech.models/v1` installed-VM gate;
- `PLEBIAN_OS_INSTALL_UV=1`;
- `PLEBIAN_OS_UV_VERSION=0.12.3`;
- `PLEBIAN_OS_UV_INSTALLER_SHA256=a7e3924ea1cd06bf1518c577d635c624ae2e2db030e0fc8ff8cf426224384e17`;
- `IMAGE_PASSWORD=plebian`; and
- `RANDOM_PASSWORD=0`.

The manifest repeats these requirements and identifies the final source
closure. On 2026-08-12 the Debian media checksum and all three snapshot
InRelease signatures, the Go archive hashes, the uv installer hash, the kitty
fallback, and both Voice downloads were independently rechecked against their
official sources. Strict artifact checksums and fresh-install/strict-media
acceptance evidence remain pending; the adjacent-upgrade evidence is recorded
above.

## Release acceptance and provenance

**PENDING.** The source and dependency closure is frozen above, and the 0.1.8
to 0.1.9 upgrade gate has passed. This section still requires the strict ISO
checksum, source archive checksum, automated acceptance JSON and checksum,
BIOS/UEFI installation records, and operator checks. Until those are recorded,
this document makes no release-artifact acceptance claim.
