[project]
name = "f107b-setup"
version = "0.0.0"
description = "F107-B first-login setup wizard and model-aware plan — pre-gate, fixture-backed construction"
requires-python = ">=3.11"
dependencies = [
  "jsonschema[format]==4.25.1",
]

[tool.uv]
package = false
