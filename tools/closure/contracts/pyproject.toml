[project]
name = "kilix-f120-contracts"
version = "0.1.0"
description = "Language-neutral F120 workspace-manifest and release-lock contract validation"
requires-python = ">=3.10"
dependencies = [
  "jsonschema[format]==4.25.1",
]

[tool.uv]
package = false
